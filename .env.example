# Copy to .env and fill in. .env is gitignored and must never be committed.
#
# On Colab the repo clone will NOT contain .env, so place a copy in Drive at:
#     MyDrive/sarvam_diarization/.env

# Step 2 - gated pyannote pipelines. Create at hf.co/settings/tokens (read scope),
# and accept the conditions on each model page.
HF_TOKEN="hf_your_token_here"

# Step 3 - Sarvam ASR.
SARVAM_API_KEY="your-key-here"

# Step 4b - per-clip script correction (Claude Sonnet 4.6 on Amazon Bedrock).
# Only needed to RE-RUN the stage; the finished transcripts are committed under
# checkpoints/asr/sarvam-saaras-v3@fusion+xlitpc/.
AWS_BEARER_TOKEN_BEDROCK="your-bedrock-bearer-token-here"
