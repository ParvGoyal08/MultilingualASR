# --- secrets: never commit ---
.env
.env.*
!.env.example
cookies.txt
*.key
*credentials*.json

# --- environments / build artifacts ---
.venv/
.work/
pipeline_out/
local_out/
# the notebook clones the repo here when run locally
sarvam-assignment/
# exported explorer: regenerate with explorer.export(); audio is never committed
error_explorer/audio/*.wav
error_explorer.zip
__pycache__/
*.pyc
.ipynb_checkpoints/
sarvam_diar.zip

# --- local editor / tool state ---
.claude/settings.local.json
.DS_Store

# Exported error-explorer corpora. explorer_assets ships the TEMPLATE only
# (index.html + audio/README.md); data/ and audio/ are generated by
# explorer.export() and are 22 MB / 1.3 GB respectively.
sarvam_diar/explorer_assets/data/
sarvam_diar/explorer_assets/audio/*.wav
error_explorer/
